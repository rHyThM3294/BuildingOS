{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p storage/app/db && touch storage/app/db/database.sqlite && php artisan migrate --force && php artisan db:seed --class='Database\\Seeders\\DemoUserSeeder' --force && php artisan l5-swagger:generate && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
